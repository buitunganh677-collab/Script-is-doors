local Puzzle = {}

-- Thuật toán giải mã số phòng 50 (Thư viện)
function Puzzle.SolveLibrary(codeTable)
    -- Giả sử codeTable chứa các cặp {Hình dạng = Số}
    -- Trả về chuỗi 5 chữ số
    local finalCode = ""
    -- Logic quét các ký hiệu trên tờ giấy (Paper)
    return finalCode
end

-- Thuật toán gạt cầu chì phòng 100
function Puzzle.ElectricWork(fuses)
    for _, fuse in pairs(fuses) do
        -- Tự động kích hoạt ProximityPrompt của cầu chì
        fireproximityprompt(fuse:FindFirstChildOfClass("ProximityPrompt"))
    end
end

return Puzzle
